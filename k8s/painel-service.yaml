apiVersion: v1
kind: Service
metadata:
  name: painel
  namespace: processador-pedidos
spec:
  type: NodePort
  selector:
    app: painel
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30001
