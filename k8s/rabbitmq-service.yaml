apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: processador-pedidos
spec:
  type: NodePort
  selector:
    app: rabbitmq
  ports:
  - name: amqp
    port: 5672
  - name: management
    port: 15672
    nodePort: 30002
